import SwiftUI

struct ProfileScreen: View {
    var body: some View {
        VStack {
            Text("ProfileScreen")
        }
        .vstackModifier()
    }
}

#Preview {
    ProfileScreen()
}
